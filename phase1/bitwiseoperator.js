//bitwise not (~)  it menas flipping all the bits at bit level 

// most imp concpet that is ~(0) = -1

console.log(~(0));


// left shift just shifts the bits at left and fill the empty place with zeros
//shortcut trick is to calculate it (number multiply by 2 ki power shift) for example 10<<1 that means (10* (2**1))
console.log(10<<1);


// right shift just shift the bits at right and remove that right digit by given shift 
// shortcut trick is to calculate it (number divide by 2 ki power shift) for example 10>>1 that means (10/ (2**1));
console.log(10>>1);